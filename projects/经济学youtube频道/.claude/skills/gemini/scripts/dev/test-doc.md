# Claude Code MCP

Claude Code is an AI coding assistant that connects to dedicated Chrome via CDP (Chrome DevTools Protocol). It can automate web interactions using real Chrome instead of launching its own browser instance.
